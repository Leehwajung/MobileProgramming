<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.lhj.simpleclac161005.MainActivity">

    <!-- 피연산자 1 -->
    <EditText
        android:id="@+id/editOperand1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/widget_margin"
        android:hint="@string/strOperand1"
        android:maxLines="1"
        android:minLines="1"
        android:inputType="number|numberSigned|numberDecimal" />

    <!-- 피연산자 2 -->
    <EditText
        android:id="@+id/editOperand2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/widget_margin"
        android:hint="@string/strOperand2"
        android:maxLines="1"
        android:minLines="1"
        android:inputType="number|numberSigned|numberDecimal"
        android:singleLine="true" />

    <!-- 더하기 버튼 -->
    <Button
        android:id="@+id/btnAdd"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/widget_margin"
        android:text="@string/strAdd" />

    <!-- 빼기 버튼 -->
    <Button
        android:id="@+id/btnSub"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/widget_margin"
        android:text="@string/strSub" />

    <!-- 곱하기 버튼 -->
    <Button
        android:id="@+id/btnMul"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/widget_margin"
        android:text="@string/strMul" />

    <!-- 나누기 버튼 -->
    <Button
        android:id="@+id/btnDiv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/widget_margin"
        android:text="@string/strDiv" />

    <!-- 나머지 값 버튼 -->
    <Button
        android:id="@+id/btnMod"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/widget_margin"
        android:text="@string/strMod" />

    <!-- 결과값 -->
    <TextView
        android:id="@+id/textResult"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_result_size"
        android:textColor="@color/colorResult"
        android:layout_margin="@dimen/widget_margin"
        android:text="@string/strResult" />
</LinearLayout>
